services:
    - type: web
      name: bars-backend
      runtime: python
      rootDir: backend/registrations
      buildCommand: pip install uv && uv sync
      startCommand: uv run uvicorn main:app --host 0.0.0.0 --port $PORT
      envVars:
          - key: PYTHON_VERSION
            value: "3.14"
