[tool:pytest]
# Backend-specific pytest configuration
# This overrides the root pyproject.toml configuration that includes lambda function paths
python_paths = .
testpaths = tests services routers/tests
addopts = -v --tb=short 