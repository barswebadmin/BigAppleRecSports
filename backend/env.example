# Big Apple Rec Sports Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Shopify Configuration
SHOPIFY_STORE=your-store.myshopify.com
SHOPIFY_TOKEN=shpat_your_shopify_access_token_here

# Slack Configuration (Production)
SLACK_REFUNDS_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_SIGNING_SECRET=your-slack-signing-secret

# Slack Configuration (Development - Optional)
# If set, these will be used when ENVIRONMENT != "production"
# If not set, will fallback to production tokens
SLACK_DEV_BOT_TOKEN=xoxb-your-dev-bot-token-here
SLACK_DEV_SIGNING_SECRET=your-dev-slack-signing-secret-here

# Shopify Webhook Configuration
SHOPIFY_WEBHOOK_SECRET=your-shopify-webhook-secret-here

# Google Apps Script Web App URLs
GAS_WAITLIST_FORM_WEB_APP_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
GAS_REFUNDS_WEBHOOK_URL=https://script.google.com/macros/s/YOUR_REFUNDS_SCRIPT_ID/exec

# Application Environment
# Controls both app configuration and API call behavior:
# - development/debug/test = Mock API calls, debug messages, broader CORS
# - production = Real API calls, production channels, restricted CORS
ENVIRONMENT=development

# Testing Configuration
# Tests will use these variables for integration testing
# If not set, tests will use safe defaults (test_token, test-store.myshopify.com)

# For production deployment on Render:
# - Set ENVIRONMENT=production
# - SHOPIFY_STORE will default to 09fe59-3.myshopify.com if not set
# - SHOPIFY_TOKEN must be set in Render environment variables
