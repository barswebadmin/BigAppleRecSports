# ============================================================================
# BARS Backend - Production Requirements
# ============================================================================
# Backend service dependencies for Render deployment
# Root directory: backend/
# ============================================================================

# ============================================================================
# PRODUCTION DEPENDENCIES
# ============================================================================
# Dependencies needed for production CLI and backend services
# These are installed when setting up the CLI via pipx

# HTTP client and utilities
requests==2.32.3
PyYAML==6.0.2

# Slack API client
slack-sdk==3.30.0
slack-bolt==1.20.1

# Google API client
google-api-python-client==2.154.0
google-auth==2.37.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# HTML parsing for scrapers
beautifulsoup4==4.12.3
lxml==5.3.0

# Configuration and environment
python-dotenv==1.0.1

# Data validation and serialization
pydantic==2.10.4

# Date/time utilities
python-dateutil==2.9.0

# ============================================================================
# SERVER DEPENDENCIES (not needed for CLI)
# ============================================================================
# FastAPI web framework and server
fastapi==0.115.6
uvicorn[standard]==0.32.1

# File upload support
python-multipart==0.0.20

# ============================================================================
# TESTING DEPENDENCIES (not needed for CLI)
# ============================================================================
# Core testing framework
pytest==8.4.1
pytest-asyncio==0.25.0
pytest-mock==3.15.0
pytest-cov>=4.0.0
pytest-clarity==1.0.1

# HTTP testing client
httpx==0.25.2

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
# This file contains backend service dependencies organized by use case:
# - PRODUCTION: Dependencies needed for CLI and backend services
# - SERVER: Dependencies only needed for FastAPI server (not CLI)
# - TESTING: Dependencies only needed for testing (not CLI)
# ============================================================================
