mutation FileCreate($files: [FileCreateInput!]!) {
  fileCreate(files: $files) {
    files {
      id
      alt
      fileStatus
      ... on MediaImage {
        image {
          ...ImageContent
        }
      }
    }
    userErrors {
      ...FilesUserError
    }
  }
}
