Status: out-of-scope
Path: backend/lib/clients/shopify_client_old/models/theme_template_models.py

Theme editor / online store domain — not the Shopify admin GQL client.
Classes: `FontWeight`, `FontStyle`, `FontConfig`, `PaddingConfig`,
`BlockSettings`, `Block`, `SectionSettings`, `Section`, `ThemeTemplate`.

Move to `backend/lib/clients/shopify_theme/` or a `theme/` module. Not part of
this consolidation.
