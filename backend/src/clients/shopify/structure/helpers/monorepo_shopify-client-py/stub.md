Status: drop
Delete: C2 (see `structure/DELETIONS.md#C2`)
Path: `lib/clients/shopify-client-py/`

Contents: one empty `README.md`. Origin unclear — probably a false start on a
`-py` naming variant that never got populated.

**Ready to delete** — no callers possible (empty file), no imports possible
(no `__init__.py`, no code). Confirmation grep: `rg "shopify-client-py"`.

| Caller | Line | What it does | Disposition |
|---|---|---|---|
| _(none expected — empty dir)_ | | | — |
