Status: keep
Path: backend/src/clients/shopify/exceptions.py

Project exceptions raised by the client (e.g. `ShopifyMutationError` for
non-empty `userErrors`). Sits alongside the codegen-generated
`generated/exceptions.py` (`GraphQLClientError`, subclasses).
