Status: keep
Path: backend/src/clients/shopify/client_base.py

Async HTTP transport for the codegen client (shop domain, access token, retry,
rate-limit handling). Wraps the codegen's generated `client_base.py` in
`generated/`.
