# ============================================================================
# BARS Project - Lambda Functions & Shared Dependencies
# ============================================================================
# This file contains dependencies for:
# - Lambda functions (lambda-functions/)
# - Shared utilities (shared-utilities/)
# - Development tooling across the project
#
# Backend dependencies: see backend/requirements.txt
# GAS dependencies: managed via Google Apps Script runtime
# ============================================================================

# ============================================================================
# LAMBDA FUNCTIONS DEPENDENCIES (lambda-functions/)
# ============================================================================
# Most lambda functions use standard library only
# Common dependencies for lambda layers and functions:

# Type hints support for older Python versions
typing-extensions>=4.5.0

# Date/time utilities (used by several lambda functions)
python-dateutil==2.9.0

# HTTP requests (used by some lambda functions)
requests==2.32.3

# Configuration and environment (used by lambda functions)
python-dotenv==1.0.1

# ============================================================================
# SHARED UTILITIES DEPENDENCIES (shared-utilities/)
# ============================================================================
# Dependencies for shared utility modules used across services

# Data validation (if needed by shared utilities)
pydantic==2.10.4

# ============================================================================
# DEVELOPMENT & TESTING DEPENDENCIES
# ============================================================================
# Testing framework for lambda functions
pytest==8.4.1
pytest-asyncio==0.25.0
pytest-mock==3.15.0
pytest-cov>=4.0.0

# HTTP testing client for integration tests
httpx==0.25.2

# ============================================================================
# GOOGLE APPS SCRIPTS (GoogleAppsScripts/)
# ============================================================================
# No Python dependencies - uses Google Apps Script runtime
# JavaScript/TypeScript dependencies managed via:
# - clasp for deployment
# - npm/package.json for TypeScript projects
# - Google Apps Script built-in libraries

# ============================================================================
# INSTALLATION INSTRUCTIONS
# ============================================================================
#
# Lambda development setup:
#   pip install -r requirements.txt
#
# Backend development:
#   cd backend && pip install -r requirements.txt
#
# Specific lambda function deployment:
#   cd lambda-functions/[function-name] && pip install -r requirements.txt
#
# Testing across project:
#   pip install -r requirements.txt  # (includes pytest and testing tools)
#
# ============================================================================
