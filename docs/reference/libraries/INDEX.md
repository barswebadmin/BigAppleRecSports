# Library / API reference index

Captured knowledge so agents don't re-research the same external library or API
every session. One file per library/API under `docs/reference/libraries/`.

Reading order when working with any of these: **local notes file → official docs
→ runtime/CLI probe → source.** Stop at the lowest tier that answers the
question. Treat this index as a cache, not a census — its silence is not proof a
capability is absent.

| file | kind | summary | last touched |
|---|---|---|---|
| [google-sheets-api.md](./google-sheets-api.md) | REST API (v4) | Values vs structural batchUpdate, A1 vs GridRange addressing, sheetId/gid stability | 2026-06-15 |
