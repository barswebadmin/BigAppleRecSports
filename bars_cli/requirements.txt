# ============================================================================
# BARS CLI - Backend Service Dependencies
# ============================================================================
# These are backend service client libraries that the CLI uses to interact
# with Google, Slack, and Shopify APIs. They are NOT included in pyproject.toml
# because they should be installed separately when needed.
# ============================================================================

# Google API clients (for Google Workspace management)
google-api-python-client==2.154.0
google-auth==2.37.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# Slack API clients (for Slack workspace management)
slack-sdk==3.30.0
slack-bolt==1.20.1

# Shopify GraphQL client (for Shopify store management)
sgqlc>=17.0

# HTML parsing (for web scraping)
beautifulsoup4==4.12.3
lxml==5.3.0

# Configuration
PyYAML==6.0.2

# HTTP client (for API calls)
requests>=2.32.3

# ============================================================================
# INSTALLATION
# ============================================================================
# Install CLI core:
#   pip install -e bars_cli/
#
# Install backend service dependencies (when needed):
#   pip install -r bars_cli/requirements.txt
# ============================================================================
