{
  "name": "bars-google-apps-scripts",
  "version": "1.0.0",
  "description": "Shared dependencies for all Google Apps Script projects",
  "private": true,
  "packageManager": "pnpm@10.16.0",
  "scripts": {
    "build": "node ../../scripts/deployment/google/build.js",
    "clean": "bash -c 'if [ -z \"$1\" ]; then echo \"❌ Usage: pnpm clean <project-name>\"; exit 1; fi; PROJECT=\"$1\"; if [ ! -d \"projects/$PROJECT\" ]; then echo \"❌ Project not found: $PROJECT\"; exit 1; fi; bash ../../scripts/deployment/google/clean.sh \"projects/$PROJECT\"' --"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "@types/google-apps-script": "^2.0.1"
  }
}
