$schema: http://json-schema.org/draft-07/schema#
title: Location
description: A specific location in a form.
type: object
properties:
  index:
    format: int32
    description: The index of an item in the form. This must be in the range [0..*N*),
      where *N* is the number of items in the form.
    type: integer
