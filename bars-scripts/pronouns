#!/bin/bash
# Bash wrapper for get_pronouns.py
# Forwards all arguments to the Python script

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/.." || exit 1

source .venv/bin/activate
python3 bars-scripts/get_pronouns.py "$@"

