mutation FileUpdate($files: [FileUpdateInput!]!) {
  fileUpdate(files: $files) {
    files {
      id
      alt
      fileStatus
      updatedAt
    }
    userErrors {
      field
      message
      code
    }
  }
}
